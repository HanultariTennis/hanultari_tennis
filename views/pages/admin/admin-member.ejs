<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>한울타리</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
  </style>
</head>
<body>
  <%- include('../../partials/header.ejs') %>
  <main>
    <div class="container mt-3">
      <div class="mb-3">
        <h5 class="d-flex justify-content-center">회원관리</h5>
      </div>
      <div class="container mb-3">
        <a href="/admin/" class="btn btn-secondary w-100 mb-3">돌아가기</a>
        <!-- <a href="/admin/member/new" class="btn btn-warning w-100 mb-3">신규회원</a> -->
      </div>
      <div class="container mb-3">
        <span>변경할 회원을 선택하세요.</span>
        <table class="table">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">이름</th>
              <th scope="col">직책</th>
              <th scope="col">생일</th>
            </tr>
          </thead>
          <tbody>
            <% members.forEach((member, index) => { %>
              <tr>
                <th scope="row"><%= index + 1 %></th>
                <td>
                  <a href="/admin/member/<%= member.name %>">
                    <strong><%= member.name %></strong>
                  </a>
                </td>
                <td><%= member.role %></td>
                <td><%= member.birth %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
        
      </div>
    </div>
    <div style="height: 100px;"></div>
  </main>
  <%- include('../../partials/footer.ejs') %>
</body>
</html>
